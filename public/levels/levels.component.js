'use strict'

angular.
	module('levels').
	component('levels', {
		template: 'levels/levels.template.html',
		controller: ['$q', '$routeParams'
			function LevelsController($q, $routeParams) {

			}
		]
	});